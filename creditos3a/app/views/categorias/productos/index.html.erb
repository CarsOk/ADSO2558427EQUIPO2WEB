<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tu Página</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application' %> <!-- aplication.css -->
</head>
<body>
<!-- Page Content -->
  <div class="container px-4 px-lg-5">
    <!-- Heading Row -->
    <div class="row gx-4 gx-lg-5 align-items-center my-5">
        <div class="col-lg-7">
            <% if @categoria.imagen.attached? %>
            <%= image_tag @categoria.imagen, class: "img-fluid rounded mb-4 mb-lg-0", alt: "Categoría" %>
          <% end %>
        </div>
        <div class="col-lg-5">
            <div class="categoria-box"> <!-- Cuadro independiente para el nombre de la categoría -->
                <h1 class="font-weight-light">Bienvenido a la Categoría:</h1>
                <h2><%= @categoria.nombre %></h2>
            </div>
            <!-- ... (Otros detalles de la categoría) ... -->
        </div>
    </div>
</div>

        <!-- Código Ruby on Rails -->
        <h4 class="display-4">Lista de Productos</h4>
        <div class="mb-4">
            <div class="row">
                <% @productos.each do |producto| %>
                    <div class="col-md-3 mb-4"> <!-- Redujimos el tamaño de las columnas a 3 para hacerlas más compactas -->
                        <div class="card h-100">
                            <%= image_tag producto.avatar.url, class: "card-img-top img-fluid" %>
                            <div class="card-body">
                                <h5 class="card-title"><%= producto.nombre %></h5> <!-- Usamos h5 para reducir el tamaño del título -->
                                <p class="card-text"><%= producto.descripcion %></p>
                                <p class="card-text"><strong>Precio: <%= number_to_currency(producto.precio) %></strong></p>
                            </div>
                            <div class="card-footer">
                                <a class="btn btn-primary btn-sm" href="#!">Más Información</a>
                            </div>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
    
    <!-- Bootstrap core JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Core theme JS -->
    <script src="js/scripts.js"></script>
</body>
</html>

